# OpenCitiVibes Frontend Configuration
# Copy to .env and customize

# ============================================
# API Connection
# ============================================
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# ============================================
# Site Configuration
# ============================================
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Fallback locale (real config comes from backend API)
NEXT_PUBLIC_DEFAULT_LOCALE=fr

# ============================================
# Environment
# ============================================
NEXT_PUBLIC_ENVIRONMENT=development

# ============================================
# Optional: Monitoring (Sentry)
# ============================================
NEXT_PUBLIC_SENTRY_DSN=
NEXT_PUBLIC_SENTRY_RELEASE=1.0.0

# Sentry Configuration (server-side / build-time)
SENTRY_ORG=your-sentry-org
SENTRY_PROJECT=idees-montreal-frontend
SENTRY_AUTH_TOKEN=your-auth-token-for-source-maps

# ============================================
# Note: Instance-specific configuration
# ============================================
# All instance branding (logo, hero, colors, name) comes from
# the backend platform config API.
#
# To switch instances:
#   ./scripts/switch-instance.sh <instance>
#
# This copies instance images to public/instance/ and configures
# the backend to use the correct database and config.
