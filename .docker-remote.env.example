# Remote VM Configuration for docker-dev.sh
# Copy this file to .docker-remote.env and customize
#
# Usage:
#   ./scripts/docker-dev.sh --remote up         # Uses config below
#   ./scripts/docker-dev.sh --remote user@host up  # Override host
#   ./scripts/docker-dev.sh --remote --sync up  # Sync files first

# SSH connection (required) - format: user@host or user@ip
DOCKER_REMOTE_HOST=user@192.168.1.100

# Project path on remote VM (default: /home/$USER/IdeesPourMontreal)
DOCKER_REMOTE_PATH=/home/user/IdeesPourMontreal

# Auto-sync project files before commands (true/false)
# Excludes: .git, node_modules, .next, __pycache__, .venv, .env, *.db
DOCKER_REMOTE_SYNC=false

# Additional SSH options (optional)
# Examples:
#   -o StrictHostKeyChecking=accept-new  # Accept new host keys
#   -i ~/.ssh/custom_key                  # Use specific SSH key
#   -p 2222                               # Non-standard SSH port
DOCKER_REMOTE_SSH_OPTS=
